{
  "name": "gehrke-webservice",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "lint": "astro check",
    "verify": "node scripts/verify.mjs",
    "test:mobile": "playwright test",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "astro": "^5.13.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@playwright/test": "^1.63.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.45.0"
  }
}
